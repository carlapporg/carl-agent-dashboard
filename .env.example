# Branch-based env (see CONTEXT.md). Never commit secrets.
#
# main        → .env.production
# other       → .env.staging
# override    → ENV_FILE=.env.local npm run dev

API_BASE_URL=
# Example when Backend is running locally:
# API_BASE_URL=http://localhost:4000/api/v1
# AUTH_STUB_MODE=true
# NEXT_PUBLIC_API_BASE_URL=
